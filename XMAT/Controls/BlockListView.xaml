<UserControl x:Class="XMAT.NetworkTrace.UI.NetworkUrlBlockerTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="5,10,5,5" TextWrapping="Wrap">
            Enter URLs to block below. You can use * as a wildcard. Examples:
            <LineBreak/>
            example.com - Blocks traffic to example.com
            <LineBreak/>
            *.example.com - Blocks traffic to all subdomains of example.com
            <LineBreak/>
            192.168.1.* - Blocks traffic to all IPs starting with 192.168.1
        </TextBlock>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewUrlTextBox" Grid.Column="0" Margin="0,0,5,0" KeyDown="NewUrlTextBox_KeyDown" 
                     VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="Add URL Block" Click="AddUrlBlock_Click" Padding="10,5"/>
        </Grid>

        <Grid Grid.Row="2" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0,0,0,5" FontWeight="Bold">Blocked URLs:</TextBlock>

            <ListBox Grid.Row="1" x:Name="BlockedUrlsListBox" 
                     ItemsSource="{Binding BlockedUrls}" 
                     BorderThickness="1" Margin="0,0,0,5"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Remove Selected" Click="RemoveUrlBlock_Click" 
                        Margin="0,0,5,0" Padding="10,5"/>
                <Button Content="Clear All" Click="ClearUrlBlocks_Click" 
                        Padding="10,5"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>