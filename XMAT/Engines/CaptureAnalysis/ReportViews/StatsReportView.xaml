<UserControl x:Class="XMAT.XboxLiveCaptureAnalysis.StatsReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XMAT.XboxLiveCaptureAnalysis"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <wv2:WebView2
            Name="webView"
            Source="{Binding ReportUrl}"
        />
    </Grid>
    <!--
    <StackPanel>
        <Button Content="STATS REPORT VIEW" Click="Button_Click"/>
        <Grid>
        <d3:Chart BottomTitle="Argument" LeftTitle="Function">
            <d3:BarGraph
                x:Name="bargraph"
                Description="Simple bargraph"
                BarsWidth="10"
                X="{Binding HorizontalValues}"
                Y="{Binding VerticalValues}">

            </d3:BarGraph>
        </d3:Chart>
        </Grid>
    </StackPanel>
    -->
</UserControl>
